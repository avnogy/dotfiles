# ~/.bashrc: executed by bash(1) for non-login shells.

# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

source "$HOME/.profile"

# History
HISTCONTROL=ignoredups:erasedups:ignoreboth # don't put duplicate lines or lines starting with space in the history.
shopt -s histappend # append to the history file, don't overwrite it
HISTSIZE=10000
HISTFILESIZE=20000
PROMPT_COMMAND="history -a; history -c; history -r; $PROMPT_COMMAND"

# Terminal
shopt -s checkwinsize # check the window size after each command and, if necessary, update the values of LINES and COLUMNS.
[[ $COLORTERM =~ ^(truecolor|24bit)$ ]] && color_prompt=yes # enable colored command prompts

# Make less more friendly for non-text input files
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# Set PS1
if [ "$color_prompt" = yes ]; then
    PS1='\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
else
    PS1='\u@\h:\w\$'
fi
unset color_prompt

# Colors
eval "$(check_and_execute dircolors)" # enable color support of ls
export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01' # colored GCC warnings and errors



[ -f ~/.aliasrc ] && source ~/.aliasrc

# enable programmable completion features 
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi
bind 'set completion-ignore-case on'

check_and_execute source "$HOME/.fzf.bash"

[ "$environment" == "wsl" ] && check_and_execute source "$HOME/.wslrc"

eval "$(~/.local/bin/zoxide init bash)"

#[ -z "$TMUX"  ] && { exec tmux new-session && exit;}

